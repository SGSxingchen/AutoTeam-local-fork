# CloudMail
CLOUDMAIL_BASE_URL=https://example.com/api
CLOUDMAIL_EMAIL=admin@example.com
CLOUDMAIL_PASSWORD=your_password
CLOUDMAIL_DOMAIN=@example.com
# Free 账号注册的独立域名（必须与 CLOUDMAIL_DOMAIN 不同，否则会被自动加入 Team）
# 留空则禁用 Free 注册功能
CLOUDMAIL_FREE_DOMAIN=
# Free 注册邮箱提供商：cloudmail 或 outlook
MAIL_PROVIDER=cloudmail

# CPA (CLIProxyAPI)
CPA_URL=http://127.0.0.1:8317
CPA_KEY=your_key

# API 鉴权（不设置则不启用，任何人都可访问）
API_KEY=

# ChatGPT Team Workspace ID（管理员登录后通常会自动识别/保存）
CHATGPT_ACCOUNT_ID=

# 5sim 接码平台（可选，用于自动手机验证）
FIVESIM_API_KEY=
FIVESIM_PRODUCT=openai
FIVESIM_COUNTRY=any
FIVESIM_OPERATOR=any

# Playwright 浏览器代理（可选）
# 示例：socks5://host.docker.internal:1080
# 注意：Playwright/Chromium 不支持带认证的 socks5（如 socks5://user:pass@host:port）
PLAYWRIGHT_PROXY_URL=
PLAYWRIGHT_PROXY_SERVER=
PLAYWRIGHT_PROXY_USERNAME=
PLAYWRIGHT_PROXY_PASSWORD=
# 示例：localhost,127.0.0.1
PLAYWRIGHT_PROXY_BYPASS=

# Free 账号注册专用代理（仅 Free 接入流程使用，Team 轮转不使用）
FREE_PLAYWRIGHT_PROXY_URL=
FREE_PLAYWRIGHT_PROXY_BYPASS=

# 轮询配置
EMAIL_POLL_INTERVAL=3
EMAIL_POLL_TIMEOUT=300
MAIL_TIMEOUT=180
REUSE_RESET_GRACE_SECONDS=300

# 自动巡检（API 模式下生效）
AUTO_CHECK_INTERVAL=300      # 巡检间隔（秒），默认 5 分钟
AUTO_CHECK_THRESHOLD=10      # 额度低于此百分比触发轮转，默认 10%
AUTO_CHECK_MIN_LOW=2         # 至少几个账号低于阈值才触发，默认 2
#
# Playwright
PLAYWRIGHT_HEADLESS=1

# WebUI 重启按钮优先使用的 systemd 服务名（可选）
AUTOTEAM_SYSTEMD_SERVICE=
